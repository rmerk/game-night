
# LiveKit WebSocket URL (public). Currently the URL is delivered via the server's
# LIVEKIT_TOKEN message; this var is reserved for a future client-side fallback.
# VITE_LIVEKIT_URL=wss://your-project.livekit.cloud
